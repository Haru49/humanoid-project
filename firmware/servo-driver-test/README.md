# Servo Driver Test

This directory is reserved for the next independent firmware experiment: safe
control of one servo through the Servo Driver Board for XIAO V1.0.

When implementation begins, the root `platformio.ini` will receive a `servo`
environment that selects only this directory's source files.

The experiment plan is:

```txt
experiments/2026-06-21-servo-driver-plan.md
```

Program code has not been added. Hardware identification and power
requirements must be confirmed first.
