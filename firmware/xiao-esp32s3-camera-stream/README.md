# XIAO ESP32-S3 Camera Wi-Fi Stream

This directory is reserved for the next camera experiment: viewing the OV3660
camera live in a web browser over local Wi-Fi.

Experiment plan:

```txt
experiments/2026-06-21-camera-wifi-stream-plan.md
```

The existing `xiao-esp32s3-camera-test` remains the known-good basic capture
experiment.

Program code has not been added. It will be written by Haruta with Claude Code
after each step is explained and approved.
